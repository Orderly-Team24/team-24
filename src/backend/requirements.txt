fastapi==0.137.2
uvicorn[standard]==0.49.0
pydantic==2.13.4
openai>=2.43,<3
python-multipart==0.0.20
# passlib 1.7.4's bcrypt self-test crashes on bcrypt>=4.0 (confirmed: still
# broken on 4.0.1) — pin below 4.0 until passlib is upgraded/replaced.
bcrypt==3.2.2
passlib[bcrypt]==1.7.4
PyJWT>=2.8

# Database
sqlalchemy>=2.0,<3
alembic>=1.13,<2
psycopg2-binary>=2.9
python-dotenv>=1.0,<2

# OCR (menu photo upload)
pytesseract>=0.3.10
Pillow>=10.0

# Test-only. Move to requirements-dev.txt when you split things up.
pytest==9.1.1
